"""Add two values after casting them to float."""


def add(a: int, b: int) -> int:
    """Add two integers and return the result as a int.

    Args:
        a (int): The first integer.
        b (int): The second integer.

    Returns:
        int: The sum of the two integers as a float.
    """
    return a + b
